// <auto-generated/>
// Contents of: hl7.fhir.r4.core version: 4.0.1
  // Primitive Naming Style: None
  // Complex Type / Resource Naming Style: PascalCase
  // Interaction Naming Style: None
  // Extension Support: NonPrimitive
// Minimum TypeScript Version: 3.7
import * as fhirModels from '../models'
/**
 * Several agents may be associated (i.e. has some responsibility for an activity) with an activity and vice-versa.
 */
export class ProvenanceAgent extends fhirModels.BackboneElement {
  /**
   * onBehalfOfIdentity should be used when the agent is not a Resource type.
   */
  onBehalfOf?: fhirModels.Reference;
  /**
   * For example: doctor, nurse, clerk, etc.
   */
  role?: fhirModels.CodeableConcept[];
  /**
   * For example: author, performer, enterer, attester, etc.
   */
  type?: fhirModels.CodeableConcept;
  /**
   * whoIdentity should be used when the agent is not a Resource type.
   */
  who: fhirModels.Reference;
  /**
   * Default constructor
   */
  constructor(source: ProvenanceAgent) {
    super(source);
    if (source["onBehalfOf"] !== undefined) { this.onBehalfOf = source.onBehalfOf; }
    if (source["role"] !== undefined) { this.role = source.role; }
    if (source["type"] !== undefined) { this.type = source.type; }
    if (source["who"] === undefined) { throw 'Missing required element who';}
    this.who = source.who;
  }
}
/**
 * An entity used in this activity.
 */
export class ProvenanceEntity extends fhirModels.BackboneElement {
  /**
   * A usecase where one Provenance.entity.agent is used where the Entity that was used in the creation/updating of the Target, is not in the context of the same custodianship as the Target, and thus the meaning of Provenance.entity.agent is to say that the entity referenced is managed elsewhere and that this Agent provided access to it.  This would be similar to where the Entity being referenced is managed outside FHIR, such as through HL7 v2, v3, or XDS. This might be where the Entity being referenced is managed in another FHIR resource server. Thus it explains the Provenance of that Entity's use in the context of this Provenance activity.
   */
  agent?: fhirModels.ProvenanceAgent[];
  /**
   * How the entity was used during the activity.
   */
  role: ProvenanceEntityRoleEnum;
  _role?: fhirModels.Element;
  /**
   * whatIdentity should be used for entities that are not a Resource type.
   */
  what: fhirModels.Reference;
  /**
   * Default constructor
   */
  constructor(source: ProvenanceEntity) {
    super(source);
    if (source["agent"] !== undefined) { this.agent = source.agent; }
    if (source["role"] === undefined) { throw 'Missing required element role';}
    this.role = source.role;
    if (source["_role"] !== undefined) { this._role = source._role; }
    if (source["what"] === undefined) { throw 'Missing required element what';}
    this.what = source.what;
  }
}
/**
 * Code Values for the Provenance.entity.role field
 */
export enum ProvenanceEntityRoleEnum {
  DERIVATION = "derivation",
  REVISION = "revision",
  QUOTATION = "quotation",
  SOURCE = "source",
  REMOVAL = "removal",
}
/**
 * Provenance of a resource is a record that describes entities and processes involved in producing and delivering or otherwise influencing that resource. Provenance provides a critical foundation for assessing authenticity, enabling trust, and allowing reproducibility. Provenance assertions are a form of contextual metadata and can themselves become important records with their own provenance. Provenance statement indicates clinical significance in terms of confidence in authenticity, reliability, and trustworthiness, integrity, and stage in lifecycle (e.g. Document Completion - has the artifact been legally authenticated), all of which may impact security, privacy, and trust policies.
 */
export class Provenance extends fhirModels.DomainResource {
  /**
   * Resource Type Name
   */
  readonly resourceType: string = "Provenance";
  /**
   * An activity is something that occurs over a period of time and acts upon or with entities; it may include consuming, processing, transforming, modifying, relocating, using, or generating entities.
   */
  activity?: fhirModels.CodeableConcept;
  /**
   * Several agents may be associated (i.e. has some responsibility for an activity) with an activity and vice-versa.
   */
  agent: fhirModels.ProvenanceAgent[];
  /**
   * An entity used in this activity.
   */
  entity?: fhirModels.ProvenanceEntity[];
  /**
   * Where the activity occurred, if relevant.
   */
  location?: fhirModels.Reference;
  /**
   * The period can be a little arbitrary; where possible, the time should correspond to human assessment of the activity time.
   */
  occurredPeriod?: fhirModels.Period;
  /**
   * The period can be a little arbitrary; where possible, the time should correspond to human assessment of the activity time.
   */
  occurredDateTime?: string;
  _occurredDateTime?: fhirModels.Element;
  /**
   * For example: Where an OAuth token authorizes, the unique identifier from the OAuth token is placed into the policy element Where a policy engine (e.g. XACML) holds policy logic, the unique policy identifier is placed into the policy element.
   */
  policy?: string[];
  _policy?: fhirModels.Element[];
  /**
   * The reason that the activity was taking place.
   */
  reason?: fhirModels.CodeableConcept[];
  /**
   * This can be a little different from the time stamp on the resource if there is a delay between recording the event and updating the provenance and target resource.
   */
  recorded: string;
  _recorded?: fhirModels.Element;
  /**
   * A digital signature on the target Reference(s). The signer should match a Provenance.agent. The purpose of the signature is indicated.
   */
  signature?: fhirModels.Signature[];
  /**
   * Target references are usually version specific, but might not be, if a version has not been assigned or if the provenance information is part of the set of resources being maintained (i.e. a document). When using the RESTful API, the identity of the resource might not be known (especially not the version specific one); the client may either submit the resource first, and then the provenance, or it may submit both using a single transaction. See the notes on transaction for further discussion.
   */
  target: fhirModels.Reference[];
  /**
   * Default constructor
   */
  constructor(source: Provenance) {
    super(source);
    if ((source['resourceType'] !== "Provenance") || (source['resourceType'] !== undefined)) { throw 'Invalid resourceType for a Provenance'; }
    if (source["activity"] !== undefined) { this.activity = source.activity; }
    if (source["agent"] === undefined) { throw 'Missing required element agent';}
    this.agent = source.agent;
    if (source["entity"] !== undefined) { this.entity = source.entity; }
    if (source["location"] !== undefined) { this.location = source.location; }
    if (source["occurredPeriod"] !== undefined) { this.occurredPeriod = source.occurredPeriod; }
    if (source["occurredDateTime"] !== undefined) { this.occurredDateTime = source.occurredDateTime; }
    if (source["_occurredDateTime"] !== undefined) { this._occurredDateTime = source._occurredDateTime; }
    if (source["policy"] !== undefined) { this.policy = source.policy; }
    if (source["_policy"] !== undefined) { this._policy = source._policy; }
    if (source["reason"] !== undefined) { this.reason = source.reason; }
    if (source["recorded"] === undefined) { throw 'Missing required element recorded';}
    this.recorded = source.recorded;
    if (source["_recorded"] !== undefined) { this._recorded = source._recorded; }
    if (source["signature"] !== undefined) { this.signature = source.signature; }
    if (source["target"] === undefined) { throw 'Missing required element target';}
    this.target = source.target;
  }
}
