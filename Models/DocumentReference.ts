// <auto-generated/>
// Contents of: hl7.fhir.r4.core version: 4.0.1
  // Primitive Naming Style: None
  // Complex Type / Resource Naming Style: PascalCase
  // Interaction Naming Style: None
  // Extension Support: NonPrimitive
// Minimum TypeScript Version: 3.7
import * as fhirModels from '../models'
/**
 * This element is labeled as a modifier because documents that append to other documents are incomplete on their own.
 */
export class DocumentReferenceRelatesTo extends fhirModels.BackboneElement {
  /**
   * If this document appends another document, then the document cannot be fully understood without also accessing the referenced document.
   */
  code: DocumentReferenceRelatesToCodeEnum;
  _code?: fhirModels.Element;
  /**
   * The target document of this relationship.
   */
  target: fhirModels.Reference;
  /**
   * Default constructor
   */
  constructor(source: DocumentReferenceRelatesTo) {
    super(source);
    if (source["code"] === undefined) { throw 'Missing required element code';}
    this.code = source.code;
    if (source["_code"] !== undefined) { this._code = source._code; }
    if (source["target"] === undefined) { throw 'Missing required element target';}
    this.target = source.target;
  }
}
/**
 * Code Values for the DocumentReference.relatesTo.code field
 */
export enum DocumentReferenceRelatesToCodeEnum {
  REPLACES = "replaces",
  TRANSFORMS = "transforms",
  SIGNS = "signs",
  APPENDS = "appends",
}
/**
 * The document and format referenced. There may be multiple content element repetitions, each with a different format.
 */
export class DocumentReferenceContent extends fhirModels.BackboneElement {
  /**
   * The document or URL of the document along with critical metadata to prove content has integrity.
   */
  attachment: fhirModels.Attachment;
  /**
   * Note that while IHE mostly issues URNs for format types, not all documents can be identified by a URI.
   */
  format?: fhirModels.Coding;
  /**
   * Default constructor
   */
  constructor(source: DocumentReferenceContent) {
    super(source);
    if (source["attachment"] === undefined) { throw 'Missing required element attachment';}
    this.attachment = source.attachment;
    if (source["format"] !== undefined) { this.format = source.format; }
  }
}
/**
 * These values are primarily added to help with searching for interesting/relevant documents.
 */
export class DocumentReferenceContext extends fhirModels.BackboneElement {
  /**
   * Describes the clinical encounter or type of care that the document content is associated with.
   */
  encounter?: fhirModels.Reference[];
  /**
   * An event can further specialize the act inherent in the type, such as  where it is simply "Procedure Report" and the procedure was a "colonoscopy". If one or more event codes are included, they shall not conflict with the values inherent in the class or type elements as such a conflict would create an ambiguous situation.
   */
  event?: fhirModels.CodeableConcept[];
  /**
   * The kind of facility where the patient was seen.
   */
  facilityType?: fhirModels.CodeableConcept;
  /**
   * The time period over which the service that is described by the document was provided.
   */
  period?: fhirModels.Period;
  /**
   * This element should be based on a coarse classification system for the class of specialty practice. Recommend the use of the classification system for Practice Setting, such as that described by the Subject Matter Domain in LOINC.
   */
  practiceSetting?: fhirModels.CodeableConcept;
  /**
   * May be identifiers or resources that caused the DocumentReference or referenced Document to be created.
   */
  related?: fhirModels.Reference[];
  /**
   * The Patient Information as known when the document was published. May be a reference to a version specific, or contained.
   */
  sourcePatientInfo?: fhirModels.Reference;
  /**
   * Default constructor
   */
  constructor(source: DocumentReferenceContext) {
    super(source);
    if (source["encounter"] !== undefined) { this.encounter = source.encounter; }
    if (source["event"] !== undefined) { this.event = source.event; }
    if (source["facilityType"] !== undefined) { this.facilityType = source.facilityType; }
    if (source["period"] !== undefined) { this.period = source.period; }
    if (source["practiceSetting"] !== undefined) { this.practiceSetting = source.practiceSetting; }
    if (source["related"] !== undefined) { this.related = source.related; }
    if (source["sourcePatientInfo"] !== undefined) { this.sourcePatientInfo = source.sourcePatientInfo; }
  }
}
/**
 * A reference to a document of any kind for any purpose. Provides metadata about the document so that the document can be discovered and managed. The scope of a document is any seralized object with a mime-type, so includes formal patient centric documents (CDA), cliical notes, scanned paper, and non-patient specific documents like policy text.
 */
export class DocumentReference extends fhirModels.DomainResource {
  /**
   * Resource Type Name
   */
  readonly resourceType: string = "DocumentReference";
  /**
   * Represents a participant within the author institution who has legally authenticated or attested the document. Legal authentication implies that a document has been signed manually or electronically by the legal Authenticator.
   */
  authenticator?: fhirModels.Reference;
  /**
   * Not necessarily who did the actual data entry (i.e. typist) or who was the source (informant).
   */
  author?: fhirModels.Reference[];
  /**
   * Key metadata element describing the the category or classification of the document. This is a broader perspective that groups similar documents based on how they would be used. This is a primary key used in searching.
   */
  category?: fhirModels.CodeableConcept[];
  /**
   * The document and format referenced. There may be multiple content element repetitions, each with a different format.
   */
  content: fhirModels.DocumentReferenceContent[];
  /**
   * These values are primarily added to help with searching for interesting/relevant documents.
   */
  context?: fhirModels.DocumentReferenceContext;
  /**
   * Identifies the logical organization (software system, vendor, or department) to go to find the current version, where to report issues, etc. This is different from the physical location (URL, disk drive, or server) of the document, which is the technical location of the document, which host may be delegated to the management of some other organization.
   */
  custodian?: fhirModels.Reference;
  /**
   * Referencing/indexing time is used for tracking, organizing versions and searching.
   */
  date?: string;
  _date?: fhirModels.Element;
  /**
   * What the document is about,  a terse summary of the document.
   */
  description?: string;
  _description?: fhirModels.Element;
  /**
   * The document that is pointed to might be in various lifecycle states.
   */
  docStatus?: DocumentReferenceDocStatusEnum;
  _docStatus?: fhirModels.Element;
  /**
   * Other identifiers associated with the document, including version independent identifiers.
   */
  identifier?: fhirModels.Identifier[];
  /**
   * CDA Document Id extension and root.
   */
  masterIdentifier?: fhirModels.Identifier;
  /**
   * This element is labeled as a modifier because documents that append to other documents are incomplete on their own.
   */
  relatesTo?: fhirModels.DocumentReferenceRelatesTo[];
  /**
   * The confidentiality codes can carry multiple vocabulary items. HL7 has developed an understanding of security and privacy tags that might be desirable in a Document Sharing environment, called HL7 Healthcare Privacy and Security Classification System (HCS). The following specification is recommended but not mandated, as the vocabulary bindings are an administrative domain responsibility. The use of this method is up to the policy domain such as the XDS Affinity Domain or other Trust Domain where all parties including sender and recipients are trusted to appropriately tag and enforce.   
   * In the HL7 Healthcare Privacy and Security Classification (HCS) there are code systems specific to Confidentiality, Sensitivity, Integrity, and Handling Caveats. Some values would come from a local vocabulary as they are related to workflow roles and special projects.
   */
  securityLabel?: fhirModels.CodeableConcept[];
  /**
   * This is the status of the DocumentReference object, which might be independent from the docStatus element.
   * This element is labeled as a modifier because the status contains the codes that mark the document or reference as not currently valid.
   */
  status: DocumentReferenceStatusEnum;
  _status?: fhirModels.Element;
  /**
   * Who or what the document is about. The document can be about a person, (patient or healthcare practitioner), a device (e.g. a machine) or even a group of subjects (such as a document about a herd of farm animals, or a set of patients that share a common exposure).
   */
  subject?: fhirModels.Reference;
  /**
   * Key metadata element describing the document that describes he exact type of document. Helps humans to assess whether the document is of interest when viewing a list of documents.
   */
  type?: fhirModels.CodeableConcept;
  /**
   * Default constructor
   */
  constructor(source: DocumentReference) {
    super(source);
    if ((source['resourceType'] !== "DocumentReference") || (source['resourceType'] !== undefined)) { throw 'Invalid resourceType for a DocumentReference'; }
    if (source["authenticator"] !== undefined) { this.authenticator = source.authenticator; }
    if (source["author"] !== undefined) { this.author = source.author; }
    if (source["category"] !== undefined) { this.category = source.category; }
    if (source["content"] === undefined) { throw 'Missing required element content';}
    this.content = source.content;
    if (source["context"] !== undefined) { this.context = source.context; }
    if (source["custodian"] !== undefined) { this.custodian = source.custodian; }
    if (source["date"] !== undefined) { this.date = source.date; }
    if (source["_date"] !== undefined) { this._date = source._date; }
    if (source["description"] !== undefined) { this.description = source.description; }
    if (source["_description"] !== undefined) { this._description = source._description; }
    if (source["docStatus"] !== undefined) { this.docStatus = source.docStatus; }
    if (source["_docStatus"] !== undefined) { this._docStatus = source._docStatus; }
    if (source["identifier"] !== undefined) { this.identifier = source.identifier; }
    if (source["masterIdentifier"] !== undefined) { this.masterIdentifier = source.masterIdentifier; }
    if (source["relatesTo"] !== undefined) { this.relatesTo = source.relatesTo; }
    if (source["securityLabel"] !== undefined) { this.securityLabel = source.securityLabel; }
    if (source["status"] === undefined) { throw 'Missing required element status';}
    this.status = source.status;
    if (source["_status"] !== undefined) { this._status = source._status; }
    if (source["subject"] !== undefined) { this.subject = source.subject; }
    if (source["type"] !== undefined) { this.type = source.type; }
  }
}
/**
 * Code Values for the DocumentReference.docStatus field
 */
export enum DocumentReferenceDocStatusEnum {
  PRELIMINARY = "preliminary",
  FINAL = "final",
  AMENDED = "amended",
  ENTERED_IN_ERROR = "entered-in-error",
}
/**
 * Code Values for the DocumentReference.status field
 */
export enum DocumentReferenceStatusEnum {
  CURRENT = "current",
  SUPERSEDED = "superseded",
  ENTERED_IN_ERROR = "entered-in-error",
}
