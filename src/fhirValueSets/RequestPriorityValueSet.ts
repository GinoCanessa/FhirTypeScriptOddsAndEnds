// <auto-generated/>
// Contents of: hl7.fhir.r4.core version: 4.0.1
// Minimum TypeScript Version: 3.7
// FHIR ValueSet: http://hl7.org/fhir/ValueSet/request-priority|4.0.1

import { Coding } from '../fhir.js'

/**
 * The clinical priority of a diagnostic order.
 */
export const RequestPriorityValueSet = {
  /**
   * asap: The request should be actioned as soon as possible - higher priority than urgent.
   */
  ASAP: new Coding({
    display: "ASAP",
    code: "asap",
    system: "http://hl7.org/fhir/request-priority",
  }),
  /**
   * routine: The request has normal priority.
   */
  Routine: new Coding({
    display: "Routine",
    code: "routine",
    system: "http://hl7.org/fhir/request-priority",
  }),
  /**
   * stat: The request should be actioned immediately - highest possible priority.  E.g. an emergency.
   */
  STAT: new Coding({
    display: "STAT",
    code: "stat",
    system: "http://hl7.org/fhir/request-priority",
  }),
  /**
   * urgent: The request should be actioned promptly - higher priority than routine.
   */
  Urgent: new Coding({
    display: "Urgent",
    code: "urgent",
    system: "http://hl7.org/fhir/request-priority",
  }),
} as const;

/**
 * The clinical priority of a diagnostic order.
 */
export type RequestPriorityValueSetType = typeof RequestPriorityValueSet;

/**
 * The clinical priority of a diagnostic order.
 */
export enum RequestPriorityValueSetEnum {
  /**
   * asap: The request should be actioned as soon as possible - higher priority than urgent.
   */
  ASAP = "asap",
  /**
   * routine: The request has normal priority.
   */
  Routine = "routine",
  /**
   * stat: The request should be actioned immediately - highest possible priority.  E.g. an emergency.
   */
  STAT = "stat",
  /**
   * urgent: The request should be actioned promptly - higher priority than routine.
   */
  Urgent = "urgent",
}
